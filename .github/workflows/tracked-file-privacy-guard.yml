name: Baseline

on:
  push:
  pull_request:

jobs:
  baseline:
    uses: eMulebb/eMule-tooling/.github/workflows/reusable-baseline.yml@ci/v7
    with:
      policy_path: manifests/privacy-guard/policy.v1.json
      repo_kind: tests
      tooling_ref: ci/v7
